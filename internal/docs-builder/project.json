{
  "name": "docs-builder",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "internal/docs-builder/src",
  "projectType": "library",
  "tags": ["skip-ci", "type:lib", "scope:internal"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "commands": [
          {
            "command": "bun run build"
          }
        ],
        "cwd": "internal/docs-builder",
        "parallel": false
      }
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bun run dev"
          }
        ],
        "cwd": "internal/docs-builder",
        "parallel": false
      }
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bun run preview"
          }
        ],
        "cwd": "internal/docs-builder",
        "parallel": false
      }
    },
    "verify": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bun run verify"
          }
        ],
        "cwd": "internal/docs-builder",
        "parallel": false
      }
    }
  }
}
