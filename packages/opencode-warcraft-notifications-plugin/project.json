{
  "name": "opencode-warcraft-notifications-plugin",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/opencode-warcraft-notifications-plugin/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bunx tsup src/index.ts --format esm --dts --out-dir dist --clean"
          }
        ],
        "cwd": "packages/opencode-warcraft-notifications-plugin",
        "parallel": false
      }
    },
    "pack": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "npm pack"
          }
        ],
        "cwd": "packages/opencode-warcraft-notifications-plugin",
        "parallel": false
      },
      "dependsOn": ["build"]
    },
    "check-mirror-exists": {
      "executor": "@pantheon-org/tools:check-mirror-exists"
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bunx eslint src/"
          }
        ],
        "cwd": "packages/opencode-warcraft-notifications-plugin",
        "parallel": false
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bunx prettier --write \"src/**/*.{ts,tsx,js,jsx,json,md}\""
          }
        ],
        "cwd": "packages/opencode-warcraft-notifications-plugin",
        "parallel": false
      }
    },
    "type-check": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bunx tsc --noEmit"
          }
        ],
        "cwd": "packages/opencode-warcraft-notifications-plugin",
        "parallel": false
      }
    },
    "dev-proxy": {
      "executor": "@pantheon-org/tools:dev-proxy",
      "options": {
        "plugins": ["opencode-warcraft-notifications-plugin"],
        "symlinkRoot": ".opencode/plugin",
        "apply": true
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          {
            "command": "bun test"
          }
        ],
        "cwd": "packages/opencode-warcraft-notifications-plugin",
        "parallel": false
      }
    }
  }
}
